import React from 'react';

const PricingCard = ({pricing}) => {
    console.log(pricing);
    return (
        <div>
            <h1>hello</h1>
            <h1>{pricing.lenght}</h1>
        </div>
    );
};

export default PricingCard;